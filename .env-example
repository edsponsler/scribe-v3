# Copy this file to .env and fill in your specific values.

# Your Google Cloud Project ID.
GOOGLE_CLOUD_PROJECT="your-gcp-project-id"
# The Google Cloud location/region for your resources (e.g., us-central1).
GOOGLE_CLOUD_LOCATION="your-gcp-location"
# The name of the Cloud Storage bucket used as the raw data lake.
# This is created in the "Create Cloud Storage Bucket" step in the README.
GCS_BUCKET_NAME="scribe-v3-regional-bucket-your-gcp-project-id"
# The numeric ID of the Vertex AI Vector Search Index.
# You can get this after running the `create_index.py` script.
VERTEX_INDEX_ID="your-vertex-index-id"
# The numeric ID of the Vertex AI Index Endpoint.
VERTEX_ENDPOINT_ID="your-vertex-endpoint-id"
